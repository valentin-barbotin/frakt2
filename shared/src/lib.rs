pub mod dtos;
pub mod env;
pub mod logger;
pub mod models;
pub mod networking;
pub mod rendering;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
